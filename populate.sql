drop table courses;

CREATE TABLE courses (
	university VARCHAR,
	campus VARCHAR,
	campus_id VARCHAR,
	term VARCHAR,
	term_id VARCHAR,
	department VARCHAR,
	department_id VARCHAR,
	course VARCHAR,
	course_id VARCHAR,
	section VARCHAR,
	section_id VARCHAR
);

select * from courses;

